# Cloud runtime settings (used when EXECUTION_MODE is unset/cloud and
# Managed Flink APPLICATION_NAME properties are not available)
execution.mode=cloud

kinesis.stream.arn=
kinesis.aws.region=
kinesis.initial.position=LATEST

iceberg.warehouse.path=
iceberg.catalog.name=
iceberg.table.name=
